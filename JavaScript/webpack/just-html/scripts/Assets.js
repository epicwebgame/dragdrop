var assets = assets || {};

function Assets() {

};

// .total
// .assetGroups.add, remove, rename, etc.
// .assetGroups[n].add(asset)
// .assetGroups[n].remove(asset)
// .assetGroups[n].assets[m].add/remove/rename, etc.